[project]
name = "noodle-api"
version = "0.0.1"
description = "Noodle API server"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "python-multipart>=0.0.9",
    "python3-saml>=1.16",
    "uvicorn[standard]>=0.29",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "alembic>=1.13",
    "redis>=5.0",
    "pydantic-settings>=2.2",
    "croniter>=2.0",
    "tzlocal>=5.0",
    "noodle-core",
    "noodle-nodes",
    "noodle-exporter",
    "noodle-importer",
    "cryptography>=42.0",
    "psutil>=5.9",
    "asyncssh>=2.14",
    "authlib>=1.3",
    "httpx>=0.27",
    "jsonschema>=4.21",
    # Sandboxed execution + docker runner pools (container spawning)
    "docker>=7.0",
    # A5: OpenTelemetry tracing (API/worker processes only — never user envs)
    "opentelemetry-sdk>=1.30",
    "opentelemetry-exporter-otlp-proto-http>=1.30",
    "opentelemetry-instrumentation-fastapi>=0.51b0",
    "opentelemetry-instrumentation-sqlalchemy>=0.51b0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[project.optional-dependencies]
kms-aws = ["boto3>=1.34"]
kms-gcp = ["google-cloud-kms>=2.21"]
kms-all = ["noodle-api[kms-aws,kms-gcp]"]
