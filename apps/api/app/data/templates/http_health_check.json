{
  "id": "http_health_check",
  "name": "HTTP health check with alert",
  "description": "Poll an endpoint on a schedule and fail the run loudly when it stops answering, so your alerting sees it.",
  "tags": [
    "monitoring",
    "http",
    "schedule",
    "starter"
  ],
  "version": "1.0.0",
  "creator": "Nodyra maintainers",
  "verified": true,
  "credential_free": true,
  "prerequisites": [
    "Outbound HTTPS to the endpoint you are checking"
  ],
  "expected_result": "A green run each time the endpoint answers healthily, and a failed run naming the status code when it does not.",
  "permissions": [
    "Network: the URL you configure"
  ],
  "compatibility": ">=1.0.0,<2.0.0",
  "screenshot_url": "/template-previews/http-health-check.svg",
  "graph": {
    "nodes": [
      {
        "id": "every_5_min",
        "type": "schedule_trigger",
        "params": {
          "interval": "minutes",
          "every": 5
        },
        "position": {
          "x": 0,
          "y": 40
        }
      },
      {
        "id": "probe",
        "type": "http_request",
        "params": {
          "url": "https://httpbin.org/json",
          "method": "GET",
          "timeout_seconds": 10,
          "max_retries": 1
        },
        "position": {
          "x": 280,
          "y": 40
        }
      },
      {
        "id": "check",
        "type": "code",
        "params": {
          "code": "# http_request raises on any non-2xx, naming the status, so\n# reaching this node already means the endpoint answered. What\n# it returns is the decoded body - there is no status_code\n# field - so assert on the body instead.\nif input in (None, '', {}, []):\n    raise ValueError('health check failed: empty response body')\noutput = {'healthy': True, 'body': input}"
        },
        "position": {
          "x": 560,
          "y": 40
        }
      }
    ],
    "edges": [
      {
        "source": "every_5_min",
        "target": "probe"
      },
      {
        "source": "probe",
        "target": "check"
      }
    ]
  }
}
